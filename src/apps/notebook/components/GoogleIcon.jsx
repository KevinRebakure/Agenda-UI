export default function GoogleIcon ({ label, className }) {
    return(
        <>
            <span className={className}>{label}</span>
        </>
    )
}

// fix material symbols outlined
// put the icon the center